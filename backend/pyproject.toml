[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "dotenv>=0.9.9",
    "fastapi>=0.125.0",
    "openai-agents>=0.6.3",
    "sentence-transformers>=5.2.0",
]
